## R CMD check results

- running `devtools::check(remote = TRUE, manual = TRUE)` gives no errors, warnings, or notes
- passing all checks on GH actions 
- running `devtools::check_win_devel()` and passes